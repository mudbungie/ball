+++
title = "bl answers no --version and has no reconciler, so a box cannot ask whether its balls binary is stale: ship --version and a make deploy-local timer like the rest of the suite"
created = 1788746064
updated = 1788746275
claimant = "Cantaloups-B2"
priority = 3
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["usability-r2"]

[[blockers]]
id = "bl-6a42"
on = "close"
+++
From the suite CD convergence (ops suite-cd.md, litany bl-8cfd): every other suite binary on a box now reports a version and rides an hourly reconciler (yog/thrall/lernie/litany+bz). bl is installed from a checkout and prints no version, so the fleet table has no row for it and 'is bl stale' is unanswerable. Ship: `bl --version` (crate version + the plugin set it was built with), and a `make deploy-local` that seats a crates.io (or checkout-main) reconciler timer with the same shape as litany's scripts/deploy/, recorded in ops suite-cd.md.